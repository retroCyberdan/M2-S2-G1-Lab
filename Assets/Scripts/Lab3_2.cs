using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Lab3_2 : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("Hello World!"); //in questa riga manda in Console il messaggio "Hello World!" UNA SOLA VOLTA (ALL'INIZIO)
    }

    // Update is called once per frame
    void Update()
    {
        Debug.Log("Hello World!"); //in questa riga manda in Console il messaggio "Hello World!" UNA VOLTA PER FRAME
    }
}
